package me.ywork.service;

import org.springframework.stereotype.Service;

@Service
public class ServerStarted  {

//	@Autowired
//	private WebApplicationContext wac;
//
//	private static SpringMvcMockDispatcher dispatcher = new SpringMvcMockDispatcher();
//
//	@Override
//	public void afterPropertiesSet() throws Exception {
//		dispatcher.setWebApplicationContext(wac);
//		if(!dispatcher.isReady()) {
//			List<Filter> filters = AppContext.getBeansForType(Filter.class);
//			dispatcher.addFilters(filters.toArray(new Filter[]{}));
//			dispatcher.build();
//			RequestDispatchers.setDispatcher(dispatcher);
//		}
//
//		// 初始化所有消息处理器
//		List<MessageHandler> messageHandlers = AppContext.getBeansForType(MessageHandler.class);
//		for (MessageHandler messageHandler : messageHandlers) {
//			//QueueEventHandler.getInstance().addHandler(messageHandler);
//			MessageHandlers.addHandler(messageHandler);
//		}
//
//		// 初始化所有kafka消息者
//		KafkaConsumerManager.getInstance().startAllControllers();
//	}

}
